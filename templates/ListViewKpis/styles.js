const styles = (theme) => ({
  container: {
    marginTop: theme.hv.spacing.lg,
  },
  controls: {
    marginTop: theme.hv.spacing.lg,
  },
  bulkActions: {
    marginTop: theme.hv.spacing.sm,
  },
  pagination: {
    marginTop: theme.hv.spacing.sm,
  },
});

export default styles;
